spring:
  application:
    name: planes
  datasource:
    url: jdbc:mysql://localhost:3306/ms-planes?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  flyway:
    enabled: true
debug: true
server:
  port: 8087
jwt:
  secret: ${JWT_SECRET}